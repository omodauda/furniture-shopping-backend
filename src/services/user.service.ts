// import User from '../interfaces/user.interface';

// // const { User: UserModel } = db;

// export default class UserService {
//   // private user = UserModel;
//   static async createUser(email: string, password: string, full_name: string): Promise<User> {
//     const response = await User.create({ email, password, full_name });
//     return response;
//   }
// }